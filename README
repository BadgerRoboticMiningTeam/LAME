To build:

mkdir build
cd build
cmake .. [-DNOGUI:BOOL=true|false]
    > set to true to build base_station component (with GUI, QT dependency)
    > set to false to build without base_station component
    > default: true